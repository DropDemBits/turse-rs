[workspace]
resolver = "2"

members = [
    "xtask/",
    "lsp-server/",
    "compiler/*",
    "fuzzers/parser_fuzz",
]

default-members = [
    "xtask/",
    "compiler/*",
    "lsp-server/",
]

[profile.release]
debug = true
