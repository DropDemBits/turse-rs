[workspace]
resolver = "2"

members = [
    "xtask/",
    "lsp-server/",
    "compiler/*",
    "fuzzers/parser_fuzz",
]

default-members = [
    "compiler/*",
    "lsp-server/",
]

[profile.release]
debug = true
